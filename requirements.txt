# Core dependencies
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# AI/ML
# UWAGA: Dla GPU wybierz jedną opcję poniżej:
#
# 1. NVIDIA GPU (RTX 4050, RTX 3000/4000 series):
#    pip uninstall torch torchvision
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 2. AMD GPU - Windows (RX 7900 GRE, RX 6000/7000):
#    pip install torch-directml
#
# 3. AMD GPU - Linux (ROCm):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm5.7
#
# 4. CPU (Domyślnie - wolniejsze):
torch>=2.0.0
torchvision>=0.15.0

ultralytics>=8.0.0  # YOLO v8

# Optional: ONNX Runtime for model optimization (uncomment if needed)
# Note: May not be available for Python 3.14+
# onnxruntime>=1.15.0

# Optional: AMD DirectML for Windows (uncomment if you have AMD GPU)
# torch-directml>=0.2.0

# OCR
pytesseract>=0.3.10
easyocr>=1.7.0

# Screen capture
mss>=9.0.1  # Cross-platform screen capture
pywin32>=306; sys_platform == 'win32'  # Windows-specific optimizations

# UI/Overlay
PyQt6>=6.5.0
pyqtgraph>=0.13.0

# Configuration & Data
pydantic>=2.0.0
pyyaml>=6.0
toml>=0.10.2

# Performance monitoring
psutil>=5.9.0
GPUtil>=1.4.0

# Async & concurrency
asyncio-mqtt>=0.16.1
aiofiles>=23.0.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
